import React, { useState } from 'react';
import './formularios.css';

export default function TelaConfiguracao({ onSave }) {
    return (
        <div className="container">
            <div className="form-container">
                <h3 className="form-title">Tela de Configuração</h3>
            </div>
        </div>
    );
}