import React, { useState } from 'react';
import './formularios.css';

export default function TelaRelatorio({ onSave }) {
    return (
        <div className="container">
            <div className="form-container">
                <h3 className="form-title">Tela de Relatorio</h3>
            </div>
        </div>
    );
}